/**
 * HTTP 错误页
 */

export default function HttpErrorsPage() {
    return (
        <div>
            <h1 className="text-2xl font-bold text-[var(--text-primary)] mb-6">HTTP 错误</h1>
            <p className="text-[var(--text-secondary)]">HTTP 错误页 - 待实现</p>
        </div>
    )
}
