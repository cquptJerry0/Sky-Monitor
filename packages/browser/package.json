{
    "name": "@sky-monitor/monitor-sdk-browser",
    "version": "1.0.0",
    "main": "build/cjs/index.js",
    "module": "build/esm/index.mjs",
    "types": "build/types/index.d.ts",
    "exports": {
        "./package.json": "./package.json",
        ".": {
            "import": {
                "types": "./build/types/index.d.ts",
                "default": "./build/esm/index.mjs"
            },
            "require": {
                "types": "./build/types/index.d.ts",
                "default": "./build/cjs/index.js"
            }
        }
    },
    "scripts": {
        "build:watch": "pnpm build:watch:transpile & pnpm build:watch:types",
        "build:watch:transpile": "tsup --watch",
        "build:watch:types": "tsc -p tsconfig.types.json --watch",
        "build": "pnpm build:transpile && pnpm build:types",
        "build:transpile": "tsup",
        "build:types": "tsc -p tsconfig.types.json",
        "clean": "rimraf build",
        "typecheck": "tsc --noEmit",
        "test": "vitest",
        "test:run": "vitest run",
        "test:watch": "vitest watch",
        "test:coverage": "vitest run --coverage",
        "test:ui": "vitest --ui"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "description": "",
    "dependencies": {
        "@sky-monitor/monitor-sdk-browser-utils": "workspace:*",
        "@sky-monitor/monitor-sdk-core": "workspace:*"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^1.0.0",
        "@vitest/ui": "^1.0.0",
        "jsdom": "^24.0.0",
        "rrweb": "2.0.0-alpha.4",
        "vitest": "^1.0.0"
    }
}
